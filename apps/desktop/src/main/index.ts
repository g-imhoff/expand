// Electron main-process entry. Window creation + IPC wiring land in Task 5.6;
// this stub gives electron-vite a resolvable `main` input and the package's
// `main: out/main/index.mjs` artifact a source.
export {}
