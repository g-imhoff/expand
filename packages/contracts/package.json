{
  "name": "@expand/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "exports": {
    "./events/domain-event": null,
    "./*": "./*.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "stage:publish": "node scripts/prepare-publish.mjs",
    "pack:tgz": "npm run build && npm run stage:publish && npm pack ./dist-publish"
  },
  "dependencies": {
    "effect": "4.0.0-beta.74"
  }
}
